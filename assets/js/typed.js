var options = {
  strings: ["an Adventurer", "a Learner", "an Explorer"], // Texts to type
  typeSpeed: 100,  // Speed of typing
  backSpeed: 50,   // Speed of deleting
  loop: true       // Loop typing animation
};

var typed = new Typed(".type", options);
